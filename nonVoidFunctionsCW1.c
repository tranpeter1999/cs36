#include <stdio.h>

float findbig(float n,float b, float c) //float is non-void
{
    float big;
        if (a>b && a>=c)
            big=a
        if (b>=a && b>=c)
            big=b;
        if (c>=a && c>=b)
            big=c;
        return big;//returns through non-void statement 
        
}

